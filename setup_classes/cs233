#!/bin/bash
ARCH="$(get_arch)"

echo "Installing Packages..."
get_packages "cs233" "iverilog-0.9.6-1 gtkwave-3.3.37-1"
if [ "$?" -ne "0" ]; then
	return 1
fi

#Don't checkout subversion twice
if [ ! -d "$HOME/svn/cs398" ]; then
	subversion_checkout "cs398" >/dev/null
	return $?
fi
